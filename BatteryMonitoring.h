  
  long batteryMonitoring(int pin){
    int batterylevel = analogRead(pin);       // read analog input pin 0
    // map values 
    //Serial.println(batterylevel, DEC);  // prints the value read
    delay(100);  

    return 0;
  }
  
